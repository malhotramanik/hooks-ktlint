name: Daily Scheduled Workflow

on:
  schedule:
    # This runs at 2:30 UTC every day
    - cron: '10 12 * * *'

jobs:
  your-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      # Add your workflow steps here
      - name: Run your task
        run: echo "Running scheduled task"
        
      # Add other steps as needed
